class CartModel {
  int id;
  String name;
  String photo;
  double price;
  int que;

  CartModel({
    required this.id,
    required this.name,
    required this.photo,
    required this.price,
    required this.que,
  });
}
